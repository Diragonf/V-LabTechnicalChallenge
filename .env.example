# Ambiente local Docker. Valores fictícios; não são credenciais de produção.
FRONTEND_PORT=8080
DB_DATABASE=vlab
DB_USERNAME=vlab
DB_PASSWORD=vlab_local_only
# Opcional: uma chave vazia é gerada em memória pelo entrypoint a cada início.
APP_KEY=
# A conexão interna é configurada pelo Compose: postgres:5432, driver pgsql.
